# Tasks — PostHog analytics — identity, funnels and errors

- T01 · Add `resolveInstallId({ stored, enabled })` to `src/main/telemetryEvents.js` returning `{ id, write }`, with tests for mint-when-absent, reuse-when-stored, reject-malformed, and null-plus-delete when telemetry is off
- T02 · Replace `@aptabase/electron` with `posthog-node` in `package.json` and `src/main/telemetry.js` — client on `https://eu.i.posthog.com` with `disableGeoip: true`, `distinctId` on every `capture`, `$set` of OS and app version on the first capture of a launch, corrected module header — leaving `track()`'s gate order, `validateEvent` and the rate limiter untouched, autocapture off, and the SDK confined to the main process
- T03 · Call `posthog.shutdown()` from the existing `before-quit` handler in `src/main/index.js` so the batcher flushes a session's final events
- T04 · Delete the stored install ID in `setEnabled(false)` and mint a fresh one on re-enable, and extend the existing fail-closed tests in `test/telemetry.test.js` to assert no ID survives while telemetry is off
- T05 · Add `sanitizeException({ message, stack })` to `src/main/telemetryEvents.js` — `scripts/redact.js` first, then absolute POSIX and Windows paths reduced to basenames — with tests covering a secret-bearing and a path-bearing input
- T06 · Add the `errorReportingEnabled` setting (default `false`) and `telemetry.captureException`, gated on that setting and on `isEnabled()` and passed through the same rate limiter instance as `track()`, then wire `src/main/crashGuard.js`'s `uncaughtException` and `unhandledRejection` handlers into it
- T07 · Add `TELEMETRY_EXCEPTION` to `src/shared/ipcChannels.js`, its `ipcMain.on` listener in `src/main/index.js` sanitizing in main, and `window.onerror` / `unhandledrejection` hooks in `src/renderer/index.js`
- T08 · Add the error-reporting row to the Privacy & Analytics section of `index.html` and wire it in `src/renderer/frameSettingsModal.js` beside the crash-dumps toggle
- T09 · Add `NOTICE_VERSION` and `shouldShowNotice({ storedVersion, currentVersion })` to `src/main/telemetryEvents.js` with tests for fresh install, legacy boolean-only client and already-seen current version, and read the versioned key in `src/renderer/telemetryNotice.js` with copy that names the install ID
- T10 · Rewrite `PRIVACY.md` for PostHog, EU residency, the install ID and what it is not, the registry's eleven events unchanged, and the error-reporting opt-in — and record the reversal of `audit-q3-product-analytics`'s vendor decision as a dated entry in `.frame/PROJECT_NOTES.md`
